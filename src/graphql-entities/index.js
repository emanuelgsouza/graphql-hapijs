const resolvers = require('./resolvers')
const typesDefsFromEntities = require('./types-defs')
const mutations = require('./mutations')

module.exports = { resolvers, typesDefsFromEntities, mutations }
